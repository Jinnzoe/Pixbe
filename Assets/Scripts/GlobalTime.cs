using UnityEngine;

public class GlobalTime : MonoBehaviour
{
    GameManager gameManager;

    // Start is called before the first frame update
    void Start()
    {
        //PlayerPrefs.GetFloat("globalTime");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
